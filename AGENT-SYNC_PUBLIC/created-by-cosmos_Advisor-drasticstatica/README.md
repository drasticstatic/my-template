# created-by-cosmos_Advisor-drasticstatica (public lane)

Publishable handoffs written by **Cosmos Advisor** running in the
**`drasticstatica (cosmos init chat)`** environment — where the software-factory fleet was
originally designed and deployed.

Sibling lane: `created-by-cosmos_Advisor-drasticstatic` (no trailing **`a`**, the working
environment). Not a typo — see
[`AGENT-SYNC/created-by-cosmos_Advisor-drasticstatica/README.md`](../../AGENT-SYNC/created-by-cosmos_Advisor-drasticstatica/README.md)
for why the two are kept distinct.

**Everything here is public.** No infrastructure detail, credentials, or client specifics. Reference
private material by pointer, never inline. The private counterpart of this lane is
`AGENT-SYNC/created-by-cosmos_Advisor-drasticstatica/`.

# created-by-Littlebird

**Agent:** Littlebird (app.littlebird.ai)
**Role:** Christopher's personal AI — context-aware assistant with screen context, calendar integration, meeting notes, and chat memory across all projects.
**Current focus:** Canonical template maintenance + Fleet coordination

> **Disclaimer: Observer-Generated Content**
> Everything in this directory is generated by LittlebirdAI — an AI assistant that observes Christopher's
> screen, calendars, meetings, and chat history. While I strive for accuracy, my observations are not 100%
> correct and may occasionally misinterpret context, miss nuance, or reflect stale assumptions. These
> handoffs are *contextual help and coordination notes*, not canonical artifacts. If anything here seems
> off, contradicts what Christopher told you directly, or feels uncertain, **consult Christopher before
> acting on it.** He is the single source of truth. I am a helpful observer doing my best to keep the fleet
> aligned.

## About this directory in the canonical template

`my-template` is the source of truth for how new repos should be bootstrapped. The `AGENT-SYNC/README.md`
here is the **canonical version** of the agent-sync convention. When Littlebird updates it here, other
repos should update their pointer copies too.

## What I do in this ecosystem

- **Fleet context bridge** — I observe Christopher's work across all repos and leave notes so agents don't
  operate in silos.
- **Convention maintainer** — I help keep the AGENT-SYNC convention current as the fleet evolves.
- **Integration layer** — Through the GitHub MCP, I can read and write files across repos, check calendars,
  and draft content in Christopher's voice.

## How other repos should use this template

When bootstrapping a new repo, copy the `AGENT-SYNC/` directory (with all `created-by-*` subdirectories)
from this template. Then replace this README with a repo-specific welcome + handoff for the agents that
will work there.

---

*This directory in `my-template` is the canonical reference. For repo-specific context, see the `created-by-Littlebird` README in the actual working repo.*
